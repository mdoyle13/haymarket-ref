<h1>Published</h1>
	<h2>Drafts</h2>
	<%= render :partial => 'beer', :collection => Beer.drafts.live unless Beer.drafts.live.nil? %>
	<br />
	<br />
	<h2>Bottles</h2>
	<%= render :partial => 'beer', :collection => Beer.bottles.live unless Beer.bottles.live.nil? %>
	<br /><br />

<br />
<br />
<br />


<h1>Not Published</h1>
	<h2>Drafts</h2>
	<%= render :partial => 'beer', :collection => Beer.drafts.pending.by_name unless Beer.drafts.pending.nil? %>
	<br />
	<br />
	<h2>Bottles</h2>
	<%= render :partial => 'beer', :collection => Beer.pending.pending.by_name unless Beer.bottles.pending.nil? %>
	<br /><br />

<br />
<br />
<br />

<h1>Unpriced</h1>
	<%= render :partial => 'beer', :collection => Beer.unpriced.by_name unless Beer.unpriced.nil? %>


<%= render :partial => "/shared/admin/sortable_list", :locals => {:continue_reordering => (defined?(continue_reordering) ? continue_reordering : true)} %>