<% content_for :body_content_left do %>
  <ul id="beers">
    <% @beers.each do |beer| %>
      <li>
        <%= link_to beer.name, beer_url(beer) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
