<div class="column" id="left_col">
	
	<% unless @brunch.foods.appetizers.published.blank? %>
	<div class="food_category">
		<h2>Appetizers</h2>
		<%= render :partial => 'food', :collection => @brunch.foods.appetizers.published unless @brunch.foods.appetizers.published.nil? %>
	</div>
	<% end %>
	
	<% unless @brunch.foods.soups_salads.published.blank? %>
	<div class="food_category">
		<h2>Soups & Salads</h2>
		<%= render :partial => 'food', :collection => @brunch.foods.soups_salads.published unless @brunch.foods.soups_salads.published.nil? %>
	</div>
	<% end %>
	
	<% unless @brunch.foods.healthier.published.blank? %>
	<div id="sides" class="food_category">
		<h2>Healthier</h2>
		<%= render :partial => 'food', :collection => @brunch.foods.healthier.published unless @brunch.foods.healthier.published.nil? %>
	</div>
	<% end %>
	
	<% unless @brunch.foods.sides.published.blank? %>
	<div id="sides" class="food_category">
		<h2>Sides</h2>
		<%= render :partial => 'food', :collection => @brunch.foods.sides.published unless @brunch.foods.sides.published.nil? %>
	</div>
	<% end %>
	
	<% unless @brunch.foods.housemade_sauces.published.blank? %>
	<div id="sauces" class="food_category">
		<h2>Housemade Sauces</h2>
		<div class="note">Extra sauces $0.50 ea</div>
		<%= render :partial => 'food', :collection => @brunch.foods.housemade_sauces.published unless @brunch.foods.housemade_sauces.published.nil? %>
	</div>
	<div class="clear"></div>
	<% end %>
	
	<% unless @brunch.foods.desserts.published.blank? %>
	<div class="food_category">
		<h2>Desserts</h2>
		<%= render :partial => 'food', :collection => @brunch.foods.desserts.published unless @brunch.foods.desserts.published.nil? %>
	</div>
	<% end %>
</div>
<div id="legend">
	<h3>Legend</h3>
	<p><%= image_tag "/theme/images/icon_housemade.gif", :class => "legend_icon" -%> = HouseMade</p>
	<p><%= image_tag "/theme/images/icon_vegan.gif", :class => "legend_icon" -%> = Vegan</p>
	<p><%= image_tag "/theme/images/icon_spicy.gif", :class => "legend_icon" -%> = Spicy</p>
</div>

<div class="column" id="right_col">
	<% unless @brunch.foods.eggs.published.blank? %>
	<div class="food_category">
		<h2>Eggs</h2>
		<%= render :partial => 'food', :collection => @brunch.foods.eggs.published unless @brunch.foods.eggs.published.nil? %>
	</div>
	<% end %>
	
	<% unless @brunch.foods.sandwiches.published.blank? %>
	<div class="food_category">
		<h2>Sandwiches</h2>
		<%= render :partial => 'food', :collection => @brunch.foods.sandwiches.published unless @brunch.foods.sandwiches.published.nil? %>
	</div>
	<% end %>
	
	<% unless @brunch.foods.french_toast.published.blank? %>
	<div class="food_category">
		<h2>French Toast</h2>
		<%= render :partial => 'food', :collection => @brunch.foods.french_toast.published unless @brunch.foods.french_toast.published.nil? %>
	</div>
	<% end %>
	
	<% unless @brunch.foods.beverages.published.blank? %>
	<div class="food_category">
		<h2>Beverages</h2>
		<%= render :partial => 'food', :collection => @brunch.foods.beverages.published unless @brunch.foods.beverages.published.nil? %>
	</div>
	<% end %>
	
</div>
<div class="clear"></div>