<% content_for :body_content_left do %>
  <ul id="presses">
    <% @presses.each do |press| %>
      <li>
        <%= link_to press.headline, press_url(press) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
