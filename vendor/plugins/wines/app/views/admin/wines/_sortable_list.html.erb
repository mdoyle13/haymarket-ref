<h1>Published</h1>
<%= render :partial => 'wine', :collection => Wine.find_all_by_published(true, :order => 'position').to_a %>
<br />
<br />
<br />
<h1>Not Published</h1>
<%= render :partial => 'wine', :collection => Wine.find_all_by_published(false, :order => 'position').to_a %>

<%= render :partial => "/shared/admin/sortable_list", :locals => {:continue_reordering => (defined?(continue_reordering) ? continue_reordering : true)} %>
